pipeline {
    agent { label 'Built-In Node' }
    stages {
        stage('Build') {
      steps {
        sh 'docker build -t roeludh/quintortest:latest .'
      }
    }
        stage('Scan') {
      steps {
        sh 'trivy roeludh/quintortest:latest'
      }
    }

    }
}
